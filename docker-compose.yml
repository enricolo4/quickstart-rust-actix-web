version: '3.6'
volumes:
  user-data:
services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_DB: test_db
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_pwd
    ports:
      - "5432:5432"