[package]
name = "rest_server"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = { version = "4.9.0" }
domain = { version = "0.1.0", path = "../../domain" }
serde = { version = "1.0.210", features = ["derive"] }
